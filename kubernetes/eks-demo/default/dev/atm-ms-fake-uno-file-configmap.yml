# Source: atm/charts/ms-fake-uno/templates/configmap-file.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: atm-ms-fake-uno-file
  labels:
    helm.sh/chart: ms-fake-uno-0.3.0
    app.kubernetes.io/name: ms-fake-uno
    app.kubernetes.io/instance: atm
    app.kubernetes.io/version: "1.0.0"
    app.kubernetes.io/managed-by: Helm
data:
  application.yml: |
    server:
      port: 8080
    datasource:
      url: ${RDS_HOST}
      password: ${DB_PASSWORD}
